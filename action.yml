name: 'action-collmex-scraper'
description: 'An action to scrape Collmex API documentation and update your CSV field mapping data'
outputs:
  DATA_CHANGED:
    description: 'Track if data has changed'
runs:
  using: 'node12'
  pre: 'src/pre.js'
  main: 'src/main.js'
  post: 'src/post.js'
